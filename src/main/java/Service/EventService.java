package Service;

import Result.EventResult;

/**
 * A service that requests an event from the database
 */
public class EventService {
    /**
     * Attempts to request an event from the database
     * @param r the request being made
     * @return An EventResult object containing the result of the attempt
     */
    public EventResult event(EventResult r) {
        return null;
    }
}
