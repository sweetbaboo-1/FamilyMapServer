package Service;

import Request.PersonRequest;
import Result.PersonResult;

/**
 * A service that fetches a single person from the database
 */
public class PersonService {
    /**
     * Attempts to fetch a single person from the database
     * @param r the PersonRequest containing the person to request
     * @return the result of the request
     */
    public PersonResult person(PersonRequest r) {
        return null;
    }
}
