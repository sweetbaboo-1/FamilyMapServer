package Service;

import Request.FamilyRequest;
import Result.FamilyResult;

/**
 * A service that tries to request an entire family from the database
 */
public class FamilyService {
    /**
     * Attempts to fetch an entire family from the database
     * @param r the FamilyRequest being made
     * @return A FamilyResult object containing the result of the attempt
     */
    public FamilyResult family(FamilyRequest r) {
        return null;
    }
}
